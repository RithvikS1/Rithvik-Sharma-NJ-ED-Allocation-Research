# Data

`raw/` holds the four public source datasets the model is built on. `processed/` holds the tables the notebooks write, including the implied-capacity table that carries results from notebook 01 into notebook 02.

Provenance for every file, and a note on the one dataset shipped as a New Jersey subset, is in [`../docs/DATA_SOURCES.md`](../docs/DATA_SOURCES.md).
